// @ts-ignore
const user = {
  task: "/bug/user/task",
  rules: "/bug/user/rules",
  suggest: "/bug/user/suggest",
  demand: "/bug/user/demand"
}

const bugRouter = {
  user
}

export default bugRouter
